# This file ensures the models/ directory is tracked by git.
# Trained model files (.joblib) are excluded via .gitignore.
# Run: python src/train.py --data data/train.csv --model all
# to populate this directory with trained models.
